[package]
name = "wav-asr"
version = "0.1.0"
edition = "2024"

[[bin]]
name = "wav_asr"
path = "src/main.rs"

[dependencies]
deli-infer = { path = "../../crates/deli-infer", features = ["cuda"] }
deli-base = { path = "../../crates/deli-base" }
hound = "3.5"
tokio = { version = "1", features = ["rt", "rt-multi-thread", "macros"] }
